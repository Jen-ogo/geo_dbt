version: 2
models:
  - name: building_footprints
    columns:
      - name: feature_id
        tests: [not_null, unique]
      - name: region_code
        tests: [not_null]
      - name: geom_wkt_4326
        tests: [not_null]
      - name: h3_r10
        tests: [not_null]

  - name: building_footprints_model
    columns:
      - name: feature_id
        tests: [not_null, unique]